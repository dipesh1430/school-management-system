{
  "name": "school-saas",
  "version": "1.0.0",
  "description": "Multi-tenant School Management SaaS",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:api": "npm run dev --workspace=apps/api"
  }
}
